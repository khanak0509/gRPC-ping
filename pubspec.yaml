name: grpc_ping
description: A small tool I built to learn gRPC channel lifecycle in Dart.
version: 0.1.0

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  args: ^2.4.2
  grpc: ^3.2.0
  protobuf: ^3.1.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

executables:
  grpc_ping: bin/grpc_ping.dart
